# 群规测验项目 · 进度快照

> 更新时间：2026-09-17 23:45
> **用途**：上下文被压缩、或换新会话后，看这一份就能接着干。

---

## 一、这是什么

给 QQ 群「**半间猫窝（Cat Ham、小猫Ham）**」（业余无线电群）做的**群规准入测验**。

新人群员答一套随机题：每题给一个真实案例，判断它属于「无违规 / 轻微违规 / 严重违规」；
答对 6 题即通过，截图凭证发群里给管理员核对。

---

## 二、当前状态（全部可用）

### 成品位置

| 文件 | 说明 |
|---|---|
| `桌面\半间猫窝群规测验.html` | **主成品 228.6 KB**，图片已 base64 内嵌 —— 双击即用、发群里不丢图 |
| `桌面\群规测验系统.zip` | 完整项目包 803 KB（含后端、随机器、全部素材） |
| `group-rules-quiz\群规问卷.html` | 源文件（外链图片版，85.5 KB） |
| `group-rules-quiz\群规问卷-单文件版.html` | 内嵌版源文件（由 `_inline2.js` 生成，脚本已删） |

### 桌面文档（独立放在桌面，方便随时翻）

| 文件 | 说明 |
|---|---|
| `README.md` | 总览：这系统干嘛的、怎么用、怎么改题 |
| `说明-接QQbot.md` | 对接文档：接口 / 接 bot / 部署 / FAQ（478 行） |
| `进度快照.md` | 本文件 |

---
### 题库

- **40 道案例三档判定题**（`题库.json`，type 全为 `case`）
- 配比：无违规 10 / 轻微违规 20 / 严重违规 10
- 每次随机抽 **10 题**（`CONFIG.drawCount`），题目与选项都打乱
- 通过条件：**总分 ≥ 60**（对 6 题）；**必答区门槛已取消**（`corePassRate: 0`）
- 依据：《半间猫窝群规》修订3（2026-08-16）
- 旧版条款题库备份：`题库-旧（条款版）.json`

### 玩法与手感

- 3D 立体选项按钮、答对粒子爆炸（**篮球 + 星星**）、音效（Web Audio 纯代码合成）
- 连击 COMBO（5 连触发彩虹描边）、彩带雨、分数滚动、按钮涟漪
- 进度条上跑着 **蔡徐坤挡板小人**（`paddle_1.png`），会上下颠
- 失败界面换成 **科比 24 号**头像（`kobe_small.png`）
- 冷幽默文案 + 按分数给评语（满分 / 稳 / 压线 / 差一点 各有说法）

### 后端（可选，未部署）

- `服务端\server.js`：零依赖 Node 服务（HTTP API + OneBot 反向 WS），端口 8788
- 接 QQ bot：反向 WS `ws://IP:8788/ws`，或 HTTP 上报 `/webhook/qq`
- 配置在 `服务端\config.json`（记得改 `adminToken`）
- **当前未运行**（已停止）

---

## 三、主人拍板的关键决策

1. **禁言档维持现状** —— 沿用群规两档（禁言1天 / 移出拉黑），不加"禁言7天"
2. **门槛拆掉** —— 让看群规的都能过
3. **题目全部改成案例三档判定**
4. **文案走冷幽默风**，让新人答得爽
5. **动效参考多邻国 + 旧时代游戏鬼畜**
6. **素材**：进度条用蔡徐坤挡板、失败界面用科比头像

---

## 四、踩过的坑（重要，别再犯）

1. **群规判例时间线 ≠ 群规版本**
   违规案例公告（7/26 ~ 8/5）多在正式群规发布前，属**已废止的临时群规**时期；
   判例里出现的「禁言 7 天」在修订3 里**没有依据** → 出题一律以修订3 为准，不引判例天数。

2. **进场动画不能用 `opacity:0 + backwards`**
   大图（830KB）加载时会拖住动画，导致内容**永久锁在透明状态**（选项文字全看不见）。
   已改成不依赖透明度的动画。

3. **Edge headless 截图的坑**
   - 窗口有最小宽度限制，390px 宽的截图会被裁 → 用 `--window-size=780 --force-device-scale-factor=2` 模拟真实手机视口
   - 要用 `Start-Process -Wait` + 独立 `--user-data-dir` 才稳定出图

4. **GitHub 下载通道**
   - `raw.githubusercontent.com` / `cdn.jsdelivr.net` / `ghproxy.net` **全部不通（000）**
   - **唯一可用：`https://gh-proxy.com/<原始raw地址>`**
   - `api.github.com` 可用（未认证限 60 次/小时），可列目录/搜仓库

5. **PowerShell 的 `-Filter` 不支持 `[]` 通配符**（曾导致临时文件没删干净、zip 臃肿）

---

## 五、下一步（待主人决定）

- [ ] 要不要做「**打砖块**」式进度条（答对打碎一块砖，素材库里已有 block*.png）
- [ ] 要不要**部署上线**（GitHub Pages / Gitee Pages / 自己的服务器）
- [ ] 要不要**接 QQ bot**（需要主人告知协议端：NapCat / Lagrange / go-cqhttp）
- [ ] 素材库还囤着：砖块 ×6、`paddle_2.png`、`ballshadow.png`、`background.jpg`（星空）

---

## 六、素材库清单（`group-rules-quiz\素材\`）

| 文件 | 用途 |
|---|---|
| `paddle_1.png` | **在用** —— 进度条上的蔡徐坤 |
| `kobe_small.png` | **在用** —— 失败界面的科比（220×220，92KB） |
| `ball.png` | **在用** —— 答对粒子里的篮球 |
| `kobe.png` | 科比原图（406KB，备用） |
| `paddle_2.png` | 蔡徐坤第二个姿势（备用） |
| `block01/02.png`、`block001/002.png` | 砖块（备用，打砖块进度条能用） |
| `ballshadow.png` | 球的投影（备用） |
| `background.jpg` | 星空背景（备用） |
